#include <string>
#include <cassert>

#include "UCI.hxx"

namespace cldr {
    // parse the input from the GUI
    void UCI::prompt(std::string input){
        // check string
        assert(!input.empty());

    }
}
